- name: 4️⃣ Instalar as ferramentas de Android
      run: |
        npm install
        npm install -g @capacitor/cli
        npx cap init "Meu n8n Mobile" "com.meun8n.app" --web-dir=. --confirm
        npx cap add android
